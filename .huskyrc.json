{
    "hooks": {
      "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true",
      "commit-msg": "commitlint --edit $1",
      "pre-commit": "lerna run precommit --parallel --stream",
      "post-commit": "git push"
    }
}
