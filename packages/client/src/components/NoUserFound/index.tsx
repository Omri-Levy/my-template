import { FunctionComponent } from 'react';
import { Heading } from '@chakra-ui/react';

const NoUserFound: FunctionComponent = () => (
  <Heading as={`h2`}>No user was found.</Heading>
);

export default NoUserFound;
